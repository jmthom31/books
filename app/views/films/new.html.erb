<div class='container'>
  <h3>Add a new film to your list</h3>

  <%= simple_form_for(@film) do |f| %>
    <%= f.input :name %>
    <%= f.input :imdb_rating %>
    <%= f.input :recommended_by %>
    <%= f.submit :Add, class: 'btn btn-primary' %>
  <% end %>

</div>
